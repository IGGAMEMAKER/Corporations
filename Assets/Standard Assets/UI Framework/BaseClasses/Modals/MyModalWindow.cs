using UnityEngine;

public class MyModalWindow : MonoBehaviour
{
    public string ModalTag;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
